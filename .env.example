# URL of your Pabbly Connect webhook workflow trigger.
# Set this in Vercel Project Settings → Environment Variables (server-side only,
# never exposed to the browser since it's only read inside api/submit.js).
PABBLY_WEBHOOK_URL=https://connect.pabbly.com/workflow/sendwebhookdata/xxxxxxxxxxxx
